<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title> 
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-5">
            <a class="text-zinc-500" href="/read">Read Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
        <form action="/Update/<%= user._id %>" method="post">
            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none" name ="name" placeholder="Name" value="<%= user.name %>" type="text">
            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none" name ="email" placeholder="Email" value="<%= user.email %>" type="text">
            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none" name ="image"  placeholder="Image url" value="<%= user.image %>" type="text">
            <input class="px-5 py-2 bg-yellow-500 text-black rounded-lg ml-3" type="submit" placeholder="Create user" value="Update User">
        </form>
        
    </div>
</body>
</html>
